# Roadmap

## In progress
- Distribution details toggle on the run page: "Show distribution details" toggle in the top-right (replacing "Mark as favorite"), off by default on every visit, confirmation modal when enabling ("Show details" / "Cancel"), no modal when disabling. Details per distribution card: students with no chosen friends in their group (plus how many friends they picked), and students grouped with someone they disliked.

## Done
- Shared-link security finding reviewed: token checks happen server-side, so the database stays closed to visitors; finding marked as not applicable.
